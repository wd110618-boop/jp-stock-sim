# 日本株 自動売買シミュレーター - 依存パッケージ一覧
#
# バージョンは「下限のみ」を指定し、上限は固定していません。
# 理由: Streamlit Community Cloud は毎回クリーンな環境でこの requirements.txt から
# インストールを行うため、上限を固定すると将来的にセキュリティ修正版等が
# 入らずビルドが失敗しやすくなります。下限のみを指定することで、
# 「本アプリが動作確認済みの最低バージョン以上」を保証しつつ、
# 互換性のある最新版を自動的に使えるようにしています。
#
# streamlit>=1.38 としているのは、本アプリで使用している
# st.dataframe/st.button 等の width="stretch" 引数（use_container_widthの後継）が
# 安定して利用できるバージョンを最低ラインとして指定するためです。

streamlit>=1.38
pandas>=2.0
numpy>=1.24
yfinance>=0.2.40
plotly>=5.18
python-dotenv>=1.0
