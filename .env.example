# 将来、証券会社APIと接続する際に使用する設定です。
# 今回のMVP（バックテスト・ペーパートレード版）では未使用ですが、
# APIキー等をソースコードに直書きしない構成のためにあらかじめ用意しています。
#
# 使い方: このファイルを .env という名前でコピーし、値を設定してください。
#   cp .env.example .env

BROKER_API_KEY=
BROKER_API_SECRET=
BROKER_ACCOUNT_ID=
